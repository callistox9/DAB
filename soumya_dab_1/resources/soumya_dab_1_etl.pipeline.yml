# The main pipeline for soumya_dab_1

resources:
  pipelines:
    soumya_dab_1_etl:
      name: soumya_dab_1_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/soumya_dab_1_etl"

      libraries:
        - glob:
            include: ../src/soumya_dab_1_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
